name: dialog_switcher
description: Allows user to switch widgets gracefully within a dialog with Animation.
version: 0.0.4
homepage: https://github.com/sarcastic-verma/dialog_switcher

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
